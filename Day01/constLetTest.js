// if(true)
// {
//     var x = 3;
// }
// console.log(x);

// if(true){
//     const y =3;

// }

// console.log(y);

// // 출력을 할 때 에러가 나는 이유 : const는 지역 변수이기 때문에 지역에서만 사용할 수 있다. 
// // var는 전역변수이기 때문에 전역에서 사용할 수 있다.

const a = 0;
a=1;

let b = 0;
b=1;
//const c;

// a는 변경이 안된다 왜냐면 const는 값이 고정값이기 때문이다. 
// b는 값이 변경되기 때문에 잘 나온다. 
// const c는 값이 없기 때문에 에러가 난다. (상수이기 때문에)
